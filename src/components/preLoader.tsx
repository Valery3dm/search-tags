import { Rotate } from '../styled';

const PreLoader: React.FC = () => (
    <div>
        <Rotate>&lt; 🌸 &gt;</Rotate>
        <div>Please enter TAG</div>
    </div>
);

export default PreLoader;
